import 'package:flutter/material.dart';

class AssistantNotification extends StatelessWidget {
  const AssistantNotification({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
